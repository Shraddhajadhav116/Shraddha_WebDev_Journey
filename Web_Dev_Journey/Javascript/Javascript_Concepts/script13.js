// Generate a random number between 1 and 100(Max).

let random_num = Math.floor(Math.random()*100)+1;

// Generate a random number between 1 and 5(Max).

let ran_num = Math.floor(Math.random()*5) + 1;
